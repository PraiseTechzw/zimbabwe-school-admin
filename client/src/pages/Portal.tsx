import { useState } from "react";
import { AlertTriangle, Award, BookOpen, CalendarDays, CircleDollarSign, ClipboardCheck, FileText, GraduationCap, HeartPulse, IdCard, LockKeyhole, Megaphone, Receipt, ShieldCheck, UserRound, Users } from "lucide-react";
import { trpc } from "@/lib/trpc";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";

const money = (minor: number, currency: string) => `${currency === "ZIG" ? "ZiG" : currency} ${(minor / 100).toFixed(2)}`;
const display = (value: string | number | null | undefined) => value || "—";
function IdentityItem({ label, value }: { label: string; value: string | number | null | undefined }) { return <div className="rounded-xl border border-slate-200 bg-white p-4"><p className="text-[11px] font-semibold uppercase tracking-[0.12em] text-slate-500">{label}</p><p className="mt-1 text-sm font-semibold text-slate-900">{display(value)}</p></div>; }

export default function Portal() {
  const [audience, setAudience] = useState<"LEARNER" | "GUARDIAN">("LEARNER");
  const [selectedLearnerId, setSelectedLearnerId] = useState<number | undefined>();
  const { data, isLoading, error } = trpc.portal.snapshot.useQuery({ audience, learnerId: selectedLearnerId });
  const selected = data?.selected;
  const identity = selected?.identity;
  return <div className="min-h-screen bg-[#f6f8fb] text-slate-900"><div className="border-b border-slate-200/70 bg-[#0f3049] px-5 py-8 text-white sm:px-8 lg:px-10"><div className="mx-auto max-w-7xl"><div className="flex flex-wrap items-end justify-between gap-6"><div><div className="mb-3 flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.22em] text-[#e4c477]"><GraduationCap className="h-4 w-4" /> Stage 10 · learner services</div><h1 className="text-3xl font-semibold tracking-tight sm:text-4xl">Your official school record, clearly presented.</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-blue-100/80">A read-only portal for registration identity, academic progress, attendance, timetable, school communication, and linked financial information.</p></div><div className="flex gap-2 rounded-2xl border border-white/10 bg-white/10 p-1.5"><Button size="sm" variant={audience === "LEARNER" ? "secondary" : "ghost"} onClick={() => { setAudience("LEARNER"); setSelectedLearnerId(undefined); }}><UserRound className="mr-2 h-4 w-4" />Learner</Button><Button size="sm" variant={audience === "GUARDIAN" ? "secondary" : "ghost"} onClick={() => { setAudience("GUARDIAN"); setSelectedLearnerId(undefined); }}><Users className="mr-2 h-4 w-4" />Guardian</Button></div></div></div></div><main className="mx-auto max-w-7xl space-y-7 px-5 py-8 sm:px-8 lg:px-10">{audience === "GUARDIAN" && <Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><Users className="h-4 w-4 text-primary" />Switch between children</CardTitle><CardDescription>Only children linked to this guardian account are available.</CardDescription></CardHeader><CardContent className="flex flex-wrap gap-2">{data?.children.map(child => <Button key={child.id} variant={selectedLearnerId === child.id || (!selectedLearnerId && data.selectedLearnerId === child.id) ? "default" : "outline"} onClick={() => setSelectedLearnerId(child.id)}>{child.name || child.studentId}</Button>)}{!data?.children.length && <p className="text-sm text-slate-500">No linked learner records were found.</p>}</CardContent></Card>}{isLoading && <Card><CardContent className="p-8 text-sm text-slate-500">Loading official school record…</CardContent></Card>}{error && <Card><CardContent className="p-8 text-sm text-red-700">{error.message}</CardContent></Card>}{identity && selected && <><Card className="overflow-hidden border-0 bg-white shadow-sm"><div className={`p-6 ${identity.registrationStatus === "ACTIVE" ? "bg-emerald-700" : "bg-amber-700"} text-white`}><div className="flex flex-wrap items-center justify-between gap-5"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-white/70">Official registration status</p><div className="mt-2 flex items-center gap-3"><h2 className="text-3xl font-semibold">{identity.registrationStatus === "ACTIVE" ? "Registered" : identity.registrationStatus}</h2><Badge className="border-white/30 bg-white/15 text-white">{identity.registrationStatus}</Badge></div><p className="mt-2 text-sm text-white/80">{identity.academicYear} · {identity.currentTerm} · {identity.currentForm}</p></div><div className="rounded-2xl border border-white/20 bg-white/10 p-4"><p className="text-xs text-white/70">Student ID</p><p className="mt-1 text-xl font-semibold">{identity.studentId}</p></div></div></div><CardContent className="grid gap-3 p-6 sm:grid-cols-2 lg:grid-cols-4"><IdentityItem label="Admission number" value={identity.admissionNumber} /><IdentityItem label="Full name" value={identity.fullName} /><IdentityItem label="Academic year" value={identity.academicYear} /><IdentityItem label="Current term" value={identity.currentTerm} /><IdentityItem label="Current form" value={identity.currentForm} /><IdentityItem label="Stream" value={identity.stream} /><IdentityItem label="Class" value={identity.className} /><IdentityItem label="Boarding / day scholar" value={identity.boardingStatus} /><IdentityItem label="House" value={identity.house} /><IdentityItem label="Status" value={identity.status} /></CardContent></Card><div className="flex items-center gap-2 rounded-2xl border border-blue-100 bg-blue-50 px-5 py-4 text-sm text-blue-900"><LockKeyhole className="h-4 w-4" /> Official Student ID, placement, marks, attendance, and registration fields are read-only in this portal.</div><Tabs defaultValue="overview" className="space-y-6"><TabsList className="h-auto w-full justify-start gap-1 overflow-x-auto rounded-2xl border border-slate-200 bg-white p-1.5"><TabsTrigger value="overview">Overview</TabsTrigger><TabsTrigger value="academic">Results & reports</TabsTrigger><TabsTrigger value="attendance">Attendance & timetable</TabsTrigger><TabsTrigger value="finance">Fees</TabsTrigger><TabsTrigger value="documents">Notices & documents</TabsTrigger></TabsList><TabsContent value="overview" className="grid gap-6 lg:grid-cols-2"><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><BookOpen className="h-4 w-4 text-primary" />Subjects</CardTitle><CardDescription>Official subjects available through the current timetable and class record.</CardDescription></CardHeader><CardContent className="flex flex-wrap gap-2">{selected.subjects.map(subject => <Badge key={subject.id} variant="secondary">{subject.name}</Badge>)}{!selected.subjects.length && <p className="text-sm text-slate-500">No subjects have been published yet.</p>}</CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><AlertTriangle className="h-4 w-4 text-primary" />Important alerts</CardTitle><CardDescription>Operational items relevant to this learner.</CardDescription></CardHeader><CardContent className="space-y-3"><div className="rounded-xl bg-slate-50 p-4 text-sm">{selected.attendance.filter(item => item.record.status === "ABSENT" || item.record.status === "LATE").length} attendance items require attention.</div><div className="rounded-xl bg-slate-50 p-4 text-sm">{selected.exeats.filter(item => item.status === "REQUESTED" || item.status === "APPROVED").length} boarding exeat request(s) are active.</div></CardContent></Card></TabsContent><TabsContent value="academic" className="grid gap-6 lg:grid-cols-2"><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><Award className="h-4 w-4 text-primary" />Results</CardTitle><CardDescription>Published subject marks and grades.</CardDescription></CardHeader><CardContent className="space-y-2">{selected.results.map(result => <div key={result.id} className="flex justify-between rounded-xl border border-slate-200 p-3 text-sm"><span>{result.subjectName}</span><span className="font-semibold">{display(result.grade)} {result.marks !== null ? `· ${result.marks}%` : ""}</span></div>)}{!selected.results.length && <p className="text-sm text-slate-500">No published results yet.</p>}</CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><FileText className="h-4 w-4 text-primary" />Report cards and SBPs</CardTitle><CardDescription>Published report cards and school-based projects.</CardDescription></CardHeader><CardContent className="space-y-2">{selected.reportCards.map(card => <div key={card.id} className="rounded-xl border border-slate-200 p-3 text-sm"><p className="font-semibold">Report card · {card.status}</p><p className="mt-1 text-xs text-slate-500">{display(card.summary)}</p></div>)}{selected.sbps.map(sbp => <div key={sbp.id} className="rounded-xl border border-slate-200 p-3 text-sm"><p className="font-semibold">SBP · {sbp.title}</p><p className="mt-1 text-xs text-slate-500">{sbp.status}{sbp.score !== null ? ` · ${sbp.score}%` : ""}</p></div>)}{!selected.reportCards.length && !selected.sbps.length && <p className="text-sm text-slate-500">No report cards or SBPs have been published yet.</p>}</CardContent></Card></TabsContent><TabsContent value="attendance" className="grid gap-6 lg:grid-cols-2"><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><HeartPulse className="h-4 w-4 text-primary" />Attendance</CardTitle><CardDescription>Daily, period, and boarding roll-call history.</CardDescription></CardHeader><CardContent className="space-y-2">{selected.attendance.slice(0, 12).map(item => <div key={item.record.id} className="flex justify-between rounded-xl border border-slate-200 p-3 text-sm"><span>{new Date(item.sessionDate).toLocaleDateString()} · {item.mode}</span><Badge variant={item.record.status === "PRESENT" ? "secondary" : "destructive"}>{item.record.status}</Badge></div>)}{!selected.attendance.length && <p className="text-sm text-slate-500">No attendance records yet.</p>}</CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><CalendarDays className="h-4 w-4 text-primary" />Timetable and assignments</CardTitle><CardDescription>Current class timetable and published assignments.</CardDescription></CardHeader><CardContent className="space-y-2">{selected.timetable.slice(0, 8).map(item => <div key={item.id} className="flex justify-between rounded-xl border border-slate-200 p-3 text-sm"><span>{item.dayOfWeek.slice(0, 3)} · {item.title}</span><span className="text-slate-500">{item.startTime}–{item.endTime}</span></div>)}{selected.assignments.slice(0, 5).map(item => <div key={item.id} className="rounded-xl border border-slate-200 p-3 text-sm"><p className="font-semibold">{item.title}</p><p className="text-xs text-slate-500">Assignment · {item.status}</p></div>)}{!selected.timetable.length && !selected.assignments.length && <p className="text-sm text-slate-500">No timetable or assignments published yet.</p>}</CardContent></Card></TabsContent><TabsContent value="finance" className="grid gap-6 lg:grid-cols-2"><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><CircleDollarSign className="h-4 w-4 text-primary" />Fee status</CardTitle><CardDescription>Balances are shown separately by currency.</CardDescription></CardHeader><CardContent className="space-y-3">{selected.finance?.balances.map(balance => <div key={balance.currency} className="rounded-xl border border-slate-200 p-4"><div className="flex items-center justify-between"><span className="font-semibold">{balance.currency === "ZIG" ? "ZiG" : balance.currency}</span><Badge variant={balance.balanceMinor > 0 ? "destructive" : "secondary"}>{balance.balanceMinor > 0 ? "Balance due" : "Clear"}</Badge></div><dl className="mt-3 space-y-1 text-sm"><div className="flex justify-between"><dt className="text-slate-500">Billed</dt><dd>{money(balance.billedMinor, balance.currency)}</dd></div><div className="flex justify-between"><dt className="text-slate-500">Paid</dt><dd>{money(balance.paidMinor, balance.currency)}</dd></div><div className="flex justify-between font-semibold"><dt>Arrears</dt><dd>{money(balance.arrearsMinor, balance.currency)}</dd></div></dl></div>)}{!selected.finance?.balances.length && <p className="text-sm text-slate-500">No financial account is linked yet.</p>}</CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><Receipt className="h-4 w-4 text-primary" />Payment history</CardTitle><CardDescription>Read-only payment receipts for this learner.</CardDescription></CardHeader><CardContent className="space-y-2">{selected.finance?.payments.slice(0, 8).map(payment => <div key={payment.id} className="flex justify-between rounded-xl border border-slate-200 p-3 text-sm"><span>{payment.receiptNumber} · {payment.method}</span><span>{money(payment.amountMinor, payment.currency)}</span></div>)}{!selected.finance?.payments.length && <p className="text-sm text-slate-500">No payment history yet.</p>}</CardContent></Card></TabsContent><TabsContent value="documents" className="grid gap-6 lg:grid-cols-2"><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><Megaphone className="h-4 w-4 text-primary" />School notices</CardTitle><CardDescription>Published notices for learners and guardians.</CardDescription></CardHeader><CardContent className="space-y-2">{selected.notices.slice(0, 8).map(notice => <div key={notice.id} className="rounded-xl border border-slate-200 p-3"><p className="text-sm font-semibold">{notice.title}</p><p className="mt-1 text-xs leading-5 text-slate-500">{notice.body}</p></div>)}{!selected.notices.length && <p className="text-sm text-slate-500">No notices published yet.</p>}</CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><IdCard className="h-4 w-4 text-primary" />Documents and exeat</CardTitle><CardDescription>Published learner documents and boarding movement status.</CardDescription></CardHeader><CardContent className="space-y-2">{selected.documents.map(document => <div key={document.id} className="flex justify-between rounded-xl border border-slate-200 p-3 text-sm"><span>{document.title}</span><Badge variant="secondary">{document.category}</Badge></div>)}{selected.exeats.map(exeat => <div key={exeat.id} className="flex justify-between rounded-xl border border-slate-200 p-3 text-sm"><span>Exeat · {exeat.destination}</span><Badge>{exeat.status}</Badge></div>)}{!selected.documents.length && !selected.exeats.length && <p className="text-sm text-slate-500">No documents or exeat records yet.</p>}</CardContent></Card></TabsContent></Tabs></>}</main></div>;
}
